import { LocalizedRouteLoading } from "@/components/localized-route-loading";

export default function LibraryLoading() {
  return <LocalizedRouteLoading label="Loading library" variant="cards" />;
}
